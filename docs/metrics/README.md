# docs/metrics/

Расход токенов и деньги. Экономия, которую не измерили, ничего не стоит.

| Файл | Что внутри | Задание |
|---|---|---|
| `tokens_d1.md` | личный замер: без инструментов → ast-index → ast-index + Caveman | 1.6.1–1.6.2 |
| командные метрики | формат уточнит ведущий | дни 2–3 |

Таблица `tokens_d1.md` — один и тот же промпт, три прогона:

| Замер | input | output | $ | ответ верный? да/нет |
|---|---|---|---|---|
| без инструментов |  |  |  |  |
| ast-index |  |  |  |  |
| ast-index + Caveman |  |  |  |  |

Под таблицей — вывод в 1–2 строки: сколько сэкономили и не потерялся ли смысл.

Колонка качества обязательна: сжатый ответ, который стал неверным, — не экономия.
Ключей и токенов в этих файлах нет. Цифры берутся из OpenRouter → Activity, а не на глаз.
